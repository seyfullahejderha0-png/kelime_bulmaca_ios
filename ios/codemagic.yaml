workflows:
  ios-workflow:
    name: LibGDX iOS Build
    environment:
      xcode: latest
      java: 17
    scripts:
      - name: Grant permission
        script: chmod +x gradlew
      - name: Build iOS
        script: ./gradlew ios:build
    artifacts:
      - ios/build/robovm/**/*.ipa